apiVersion: v1
kind: Pod
metadata:
  name: simple-webapp-color
spec:
  containers:
  - name: simple-webapp-color
    image: naveengogu/python-web-app:v1
    ports:
      - containerPort: 8080
    env:
      - name: APP_COLOR
        value: pink
